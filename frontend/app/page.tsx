import Link from 'next/link'

export default function LandingPage(){
 return <main><section className="hero"><div><span className="badge">Graduation project platform</span><h1>AI-powered preprocessing for real datasets.</h1><p>Upload CSV or Excel files, profile schemas, validate quality, generate smart preprocessing plans, and track every run through a Supabase-backed web app.</p><div style={{display:'flex',gap:12,marginTop:24,flexWrap:'wrap'}}><Link className="button" href="/login">Start building</Link><Link className="button secondary" href="/workspace">Open workspace</Link></div></div><div className="card stack"><h2>End-to-end workflow</h2>{['Upload dataset','Extract schema and statistics','Validate data quality','Generate AI preprocessing plan','Save runs to Supabase','Review history and outputs'].map((x,i)=><div key={x} className="pill">{i+1}. {x}</div>)}</div></section><section className="grid grid-3"><div className="card"><h3>Supabase database</h3><p className="muted">PostgreSQL tables for datasets, plans, and pipeline runs with user ownership.</p></div><div className="card"><h3>FastAPI backend</h3><p className="muted">Your existing Python pipeline is exposed through production-style API endpoints.</p></div><div className="card"><h3>Next.js frontend</h3><p className="muted">A real dashboard, login page, upload workspace, and run history.</p></div></section></main>
}
